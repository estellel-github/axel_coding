/*
Write a JavaScript function named add that takes two parameters (numbers)
and returns their sum.
● Call the add function with sample numbers and print the result to the
console.
*/

function add(num1, num2) {
  return num1 + num2;
}

console.log(add(1, 5));
